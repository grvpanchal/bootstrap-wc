---
title: Rickshaw
description: D3-based time-series visualisations.
sidebar:
  label: Rickshaw
---

import Example from '../../components/Example.astro';

{/* Live preview wiring: upstream library + plugin define from CDN. */}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/rickshaw@1.7.1/rickshaw.min.css" />
<script is:inline src="https://d3js.org/d3.v3.min.js"></script>
<script is:inline src="https://cdn.jsdelivr.net/npm/rickshaw@1.7.1/rickshaw.min.js"></script>
<script is:inline type="module" src="https://esm.sh/@bootstrap-wc/plugin-rickshaw@1.0.0/define"></script>

`<bs-rickshaw-chart>` wraps [Rickshaw](https://tech.shutterstock.com/rickshaw/) so you can drop it into any
HTML page like a regular custom element.

Wraps [Rickshaw](https://tech.shutterstock.com/rickshaw/) as a Lit-based web component. Re-dispatches the upstream lifecycle as standard `bs:*` custom events. Renders in **light DOM** so the upstream library's CSS reaches its children.

## Live example

<Example code={`<bs-rickshaw-chart options='{"renderer":"line","width":540,"height":200,"series":[{"data":[{"x":0,"y":40},{"x":1,"y":49},{"x":2,"y":17},{"x":3,"y":42},{"x":4,"y":31}],"color":"#0d6efd","name":"Visits"}]}'>
  <div></div>
</bs-rickshaw-chart>`} />

## Install

```sh
npm install @bootstrap-wc/plugin-rickshaw rickshaw
```

The plugin lists the upstream library as a peer dependency
(`rickshaw@^1.7.1`) — install it alongside the wrapper.

## Loading

Load the upstream library and the plugin's `define` entry. Order matters —
the plugin reads `globalThis` for its dependencies on connect.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/rickshaw@1.7.1/rickshaw.min.css">
<script src="https://d3js.org/d3.v3.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/rickshaw@1.7.1/rickshaw.min.js"></script>

<!-- bootstrap-wc plugin (registers <bs-rickshaw-chart>) -->
<script type="module"
  src="https://esm.sh/@bootstrap-wc/plugin-rickshaw/define"></script>
```

Or via npm:

```ts
import 'rickshaw';
import '@bootstrap-wc/plugin-rickshaw/define';
```

## Listening for events

```js
const el = document.querySelector('bs-rickshaw-chart');
el.addEventListener('bs:ready', (e) => {
  // e.detail.instance is the underlying Rickshaw instance
  console.log('ready', e.detail.instance);
});
```

## Manual control

Set `auto="false"` to suppress automatic instantiation, then drive the
lifecycle yourself.

```js
const el = document.querySelector('bs-rickshaw-chart');
el.setAttribute('auto', 'false');
el.options = { /* … */ };
el.instantiate();   // create the upstream instance
el.dispose();       // destroy it
```

## API

### Attributes / properties

| Name | Type | Default | Description |
|---|---|---|---|
| options | `object` | `{}` | Plain object passed as upstream library config (also accepts JSON string attribute). |
| auto | `boolean` | `true` | Auto-instantiate on connect. Set to `false` to opt into manual control. |

### Events

| Name | Detail | When |
|---|---|---|
| bs:ready | `{ instance }` | Fired after the upstream library is initialized. |
| bs:change | `{ instance, type, raw }` | Re-dispatched upstream events (e.g. `select`, `submit`). |
| bs:destroy | — | Fired before the upstream instance is torn down. |

### Slots

| Slot | Description |
|---|---|
| _(default)_ | Markup the upstream library operates on (canvas, table, input, etc.). |

## Compatibility

- Lives in **light DOM** so plugin CSS reaches the slotted children.
- Place it inside other `<bs-*>` components only when those expose **named
  slots** that participate in the light tree — shadow-root content is invisible
  to the upstream library.
- Avoid `document`-wide jQuery selectors that assume the plugin DOM is at the
  page root; scope to the wrapper instead.

See `packages/PLUGINS.md` in the source for the full plugin compatibility
matrix.
