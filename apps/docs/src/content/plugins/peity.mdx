---
title: Peity
description: Tiny inline progress, line, bar and pie charts.
sidebar:
  label: Peity
---

import Example from '../../components/Example.astro';

{/* Live preview wiring: upstream library loaded from CDN. The bs-peity-chart */}
{/* element is registered by Example.astro's bundled plugin defines. */}
<script is:inline src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script is:inline src="https://cdn.jsdelivr.net/npm/peity@3.3.0/jquery.peity.min.js"></script>

`<bs-peity-chart>` wraps [Peity](http://benpickles.github.io/peity/) so you can drop it into any
HTML page like a regular custom element.

Renders in **light DOM** so the upstream library's CSS reaches its slotted
children. Re-dispatches the upstream lifecycle as standard `bs:*` custom
events.

## Live example

<Example code={`<p>Sales pipeline:
  <bs-peity-chart options='{"type":"line","width":80,"height":24,"stroke":"#0d6efd"}'>
    <span>5,3,9,6,5,9,7,3,5,2</span>
  </bs-peity-chart>
  / Conversions:
  <bs-peity-chart options='{"type":"bar","width":80,"height":24,"fill":["#198754"]}'>
    <span>5,3,9,6,5,9,7,3</span>
  </bs-peity-chart>
  / Mix:
  <bs-peity-chart options='{"type":"pie","fill":["#0d6efd","#dee2e6"]}'>
    <span>3/8</span>
  </bs-peity-chart>
</p>`} />

## Install

```sh
npm install @bootstrap-wc/plugin-peity peity
```

The plugin lists the upstream library as a peer dependency
(`peity@^3.3.0`) — install it alongside the wrapper.

## Loading

Load the upstream library and the plugin's `define` entry. Order matters —
the plugin reads `globalThis` for its dependencies on connect.

```html
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/peity@3.3.0/jquery.peity.min.js"></script>

<!-- bootstrap-wc plugin (registers <bs-peity-chart>) -->
<script type="module"
  src="https://esm.sh/@bootstrap-wc/plugin-peity/define"></script>
```

Or via npm:

```ts
import 'peity';
import '@bootstrap-wc/plugin-peity/define';
```

## Listening for events

```js
const el = document.querySelector('bs-peity-chart');
el.addEventListener('bs:ready', (e) => {
  // e.detail.instance is the underlying Peity instance
  console.log('ready', e.detail.instance);
});
```

## Manual control

Set `auto="false"` to suppress automatic instantiation, then drive the
lifecycle yourself.

```js
const el = document.querySelector('bs-peity-chart');
el.setAttribute('auto', 'false');
el.options = { /* … */ };
el.connectedCallback();   // re-runs instantiate when auto is later flipped
```

## API

### Attributes / properties

| Name | Type | Default | Description |
|---|---|---|---|
| options | `object` | `{}` | Plain object passed as upstream library config (also accepts JSON string attribute). |
| auto | `boolean` | `true` | Auto-instantiate on connect. Set to `false` to opt into manual control. |

### Events

| Name | Detail | When |
|---|---|---|
| bs:ready | `{ instance, target }` | Fired after the upstream library is initialized. |
| bs:destroy | — | Fired before the upstream instance is torn down. |

### Slots

| Slot | Description |
|---|---|
| _(default)_ | Markup the upstream library operates on (canvas, table, input, etc.). |

## Compatibility

- Lives in **light DOM** so plugin CSS reaches the slotted children.
- Place it inside other `<bs-*>` components only when those expose **named
  slots** that participate in the light tree — shadow-root content is invisible
  to the upstream library.
- Avoid `document`-wide jQuery selectors that assume the plugin DOM is at the
  page root; scope to the wrapper instead.
