---
title: SmartWizard
description: Multi-step form / wizard with progress and validation hooks.
sidebar:
  label: SmartWizard
---

import Example from '../../components/Example.astro';

{/* Live preview wiring: upstream library loaded from CDN. The bs-wizard */}
{/* element is registered by Example.astro's bundled plugin defines. */}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/smartwizard@6.0.6/dist/css/smart_wizard_all.min.css" />
<script is:inline src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script is:inline src="https://cdn.jsdelivr.net/npm/smartwizard@6.0.6/dist/js/jquery.smartWizard.min.js"></script>

`<bs-wizard>` wraps [SmartWizard 6](https://techlaboratory.net/jquery-smartwizard) so you can drop it into any
HTML page like a regular custom element.

Renders in **light DOM** so the upstream library's CSS reaches its slotted
children. Re-dispatches the upstream lifecycle as standard `bs:*` custom
events.

## Live example

<Example code={`<bs-wizard options='{"theme":"arrows","autoAdjustHeight":true}'>
  <div>
    <ul class="nav">
      <li class="nav-item"><a class="nav-link" href="#step-1">Step 1<br><small>Account</small></a></li>
      <li class="nav-item"><a class="nav-link" href="#step-2">Step 2<br><small>Profile</small></a></li>
      <li class="nav-item"><a class="nav-link" href="#step-3">Step 3<br><small>Done</small></a></li>
    </ul>
    <div class="tab-content">
      <div id="step-1" class="tab-pane">First step content. Click Next →</div>
      <div id="step-2" class="tab-pane">Second step content.</div>
      <div id="step-3" class="tab-pane">All done!</div>
    </div>
  </div>
</bs-wizard>`} />

## Install

```sh
npm install @bootstrap-wc/plugin-wizard smartwizard
```

The plugin lists the upstream library as a peer dependency
(`smartwizard@^6.0.6`) — install it alongside the wrapper.

## Loading

Load the upstream library and the plugin's `define` entry. Order matters —
the plugin reads `globalThis` for its dependencies on connect.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/smartwizard@6.0.6/dist/css/smart_wizard_all.min.css">
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/smartwizard@6.0.6/dist/js/jquery.smartWizard.min.js"></script>

<!-- bootstrap-wc plugin (registers <bs-wizard>) -->
<script type="module"
  src="https://esm.sh/@bootstrap-wc/plugin-wizard/define"></script>
```

Or via npm:

```ts
import 'smartwizard';
import '@bootstrap-wc/plugin-wizard/define';
```

## Listening for events

```js
const el = document.querySelector('bs-wizard');
el.addEventListener('bs:ready', (e) => {
  // e.detail.instance is the underlying SmartWizard 6 instance
  console.log('ready', e.detail.instance);
});
```

## Manual control

Set `auto="false"` to suppress automatic instantiation, then drive the
lifecycle yourself.

```js
const el = document.querySelector('bs-wizard');
el.setAttribute('auto', 'false');
el.options = { /* … */ };
el.connectedCallback();   // re-runs instantiate when auto is later flipped
```

## API

### Attributes / properties

| Name | Type | Default | Description |
|---|---|---|---|
| options | `object` | `{}` | Plain object passed as upstream library config (also accepts JSON string attribute). |
| auto | `boolean` | `true` | Auto-instantiate on connect. Set to `false` to opt into manual control. |

### Events

| Name | Detail | When |
|---|---|---|
| bs:ready | `{ instance, target }` | Fired after the upstream library is initialized. |
| bs:destroy | — | Fired before the upstream instance is torn down. |

### Slots

| Slot | Description |
|---|---|
| _(default)_ | Markup the upstream library operates on (canvas, table, input, etc.). |

## Compatibility

- Lives in **light DOM** so plugin CSS reaches the slotted children.
- Place it inside other `<bs-*>` components only when those expose **named
  slots** that participate in the light tree — shadow-root content is invisible
  to the upstream library.
- Avoid `document`-wide jQuery selectors that assume the plugin DOM is at the
  page root; scope to the wrapper instead.
