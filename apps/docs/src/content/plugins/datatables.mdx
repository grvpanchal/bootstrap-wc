---
title: DataTables
description: DataTables wrapper with sortable, searchable, paginated tables.
sidebar:
  label: DataTables
---

import Example from '../../components/Example.astro';

{/* Live preview wiring: upstream library + plugin define from CDN. */}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/datatables.net-bs5@1.13.11/css/dataTables.bootstrap5.min.css" />
<script is:inline src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script is:inline src="https://cdn.jsdelivr.net/npm/datatables.net@1.13.11/js/jquery.dataTables.min.js"></script>
<script is:inline src="https://cdn.jsdelivr.net/npm/datatables.net-bs5@1.13.11/js/dataTables.bootstrap5.min.js"></script>
<script is:inline type="module" src="https://esm.sh/@bootstrap-wc/plugin-datatables@1.0.0/define"></script>

`<bs-datatable>` wraps [DataTables](https://datatables.net/) so you can drop it into any
HTML page like a regular custom element.

Wraps [DataTables](https://datatables.net/) as a Lit-based web component. Re-dispatches the upstream lifecycle as standard `bs:*` custom events. Renders in **light DOM** so the upstream library's CSS reaches its children.

## Live example

<Example code={`<bs-datatable options='{"paging":true,"searching":true,"info":false}'>
  <table class="table table-striped">
    <thead>
      <tr><th>Name</th><th>Role</th><th>Joined</th></tr>
    </thead>
    <tbody>
      <tr><td>Jane Doe</td><td>Designer</td><td>2024-01-15</td></tr>
      <tr><td>John Smith</td><td>Engineer</td><td>2023-08-22</td></tr>
      <tr><td>Ada Lovelace</td><td>Architect</td><td>2022-11-03</td></tr>
      <tr><td>Linus T.</td><td>Maintainer</td><td>2021-04-19</td></tr>
    </tbody>
  </table>
</bs-datatable>`} />

## Install

```sh
npm install @bootstrap-wc/plugin-datatables datatables.net
```

The plugin lists the upstream library as a peer dependency
(`datatables.net@^1.13.11`) — install it alongside the wrapper.

## Loading

Load the upstream library and the plugin's `define` entry. Order matters —
the plugin reads `globalThis` for its dependencies on connect.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/datatables.net-bs5@1.13.11/css/dataTables.bootstrap5.min.css">
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/datatables.net@1.13.11/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/datatables.net-bs5@1.13.11/js/dataTables.bootstrap5.min.js"></script>

<!-- bootstrap-wc plugin (registers <bs-datatable>) -->
<script type="module"
  src="https://esm.sh/@bootstrap-wc/plugin-datatables/define"></script>
```

Or via npm:

```ts
import 'datatables.net';
import '@bootstrap-wc/plugin-datatables/define';
```

## Listening for events

```js
const el = document.querySelector('bs-datatable');
el.addEventListener('bs:ready', (e) => {
  // e.detail.instance is the underlying DataTables instance
  console.log('ready', e.detail.instance);
});
```

## Manual control

Set `auto="false"` to suppress automatic instantiation, then drive the
lifecycle yourself.

```js
const el = document.querySelector('bs-datatable');
el.setAttribute('auto', 'false');
el.options = { /* … */ };
el.instantiate();   // create the upstream instance
el.dispose();       // destroy it
```

## API

### Attributes / properties

| Name | Type | Default | Description |
|---|---|---|---|
| options | `object` | `{}` | Plain object passed as upstream library config (also accepts JSON string attribute). |
| auto | `boolean` | `true` | Auto-instantiate on connect. Set to `false` to opt into manual control. |

### Events

| Name | Detail | When |
|---|---|---|
| bs:ready | `{ instance }` | Fired after the upstream library is initialized. |
| bs:change | `{ instance, type, raw }` | Re-dispatched upstream events (e.g. `select`, `submit`). |
| bs:destroy | — | Fired before the upstream instance is torn down. |

### Slots

| Slot | Description |
|---|---|
| _(default)_ | Markup the upstream library operates on (canvas, table, input, etc.). |

## Compatibility

- Lives in **light DOM** so plugin CSS reaches the slotted children.
- Place it inside other `<bs-*>` components only when those expose **named
  slots** that participate in the light tree — shadow-root content is invisible
  to the upstream library.
- Avoid `document`-wide jQuery selectors that assume the plugin DOM is at the
  page root; scope to the wrapper instead.

See `packages/PLUGINS.md` in the source for the full plugin compatibility
matrix.
