---
title: bs-select
description: Form-associated select.
---

import Example from '../../../components/Example.astro';

Form-associated select.

## Example

<Example code={`<bs-select></bs-select>`} />

## Install

```sh
# as an npm package
npm install @bootstrap-wc/components
# or copy the source
npx bwc add select
```



## Usage

```ts
import '@bootstrap-wc/components/select';
```

```html
<bs-select></bs-select>
```

See `packages/components/src/select/` in the source for the complete property / event list.
