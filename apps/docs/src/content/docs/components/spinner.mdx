---
title: bs-spinner
description: Loading spinner (border or grow).
---

import Example from '../../../components/Example.astro';

Loading spinner (border or grow).

## Example

<Example code={`<bs-spinner variant="primary"></bs-spinner>`} />

## Install

```sh
# as an npm package
npm install @bootstrap-wc/components
# or copy the source
npx bwc add spinner
```



## Usage

```ts
import '@bootstrap-wc/components/spinner';
```

```html
<bs-spinner variant="primary"></bs-spinner>
```

See `packages/components/src/spinner/` in the source for the complete property / event list.
