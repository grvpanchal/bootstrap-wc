---
title: bs-form-text
description: Help / valid / invalid feedback text.
---

import Example from '../../../components/Example.astro';

Help / valid / invalid feedback text.

## Example

<Example code={`<bs-form-text>We never share your email.</bs-form-text>`} />

## Install

```sh
# as an npm package
npm install @bootstrap-wc/components
# or copy the source
npx bwc add form-text
```



## Usage

```ts
import '@bootstrap-wc/components/form-text';
```

```html
<bs-form-text>We never share your email.</bs-form-text>
```

See `packages/components/src/form-text/` in the source for the complete property / event list.
