---
// `<bs-*>` port of ./reference.astro for the Navbar Fixed example.
// Uses <bs-navbar fixed> with its built-in toggler / responsive collapse —
// no bootstrap.bundle.min.js required.
---

<!doctype html>
<html lang="en" data-bs-theme="light">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Navbar fixed wc</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <style>
      body {
        font-family:
          system-ui,
          -apple-system,
          sans-serif;
        padding-top: 4.5rem;
        min-height: 75rem;
      }
    </style>
    <script>
      import('@bootstrap-wc/components');
    </script>
  </head>
  <body data-compare-key="root">
    <bs-navbar expand="md" theme="dark" background="dark" placement="fixed-top" data-compare-key="navbar">
      <span slot="brand">Fixed navbar</span>
      <bs-nav class="me-auto mb-2 mb-md-0" data-compare-key="navbar-nav">
        <bs-nav-item href="#" active>Home</bs-nav-item>
        <bs-nav-item href="#">Link</bs-nav-item>
        <bs-nav-item disabled>Disabled</bs-nav-item>
      </bs-nav>
      <form slot="end" class="d-flex" role="search" data-compare-key="navbar-form">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" />
        <bs-button variant="success" button-style="outline" type="submit" data-compare-key="navbar-form-submit">Search</bs-button>
      </form>
    </bs-navbar>

    <main class="container">
      <div class="bg-body-tertiary p-5 rounded" data-compare-key="hero">
        <h1 data-compare-key="hero-title">Navbar example</h1>
        <p class="lead" data-compare-key="hero-lead">This example is a quick exercise to illustrate how fixed to top navbar works. As you scroll, it will remain fixed to the top of your browser's viewport.</p>
        <bs-button variant="primary" size="lg" href="#" data-compare-key="hero-cta">View navbar docs &raquo;</bs-button>
      </div>
    </main>
  </body>
</html>
