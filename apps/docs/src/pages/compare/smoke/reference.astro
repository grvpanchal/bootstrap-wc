---
// Self-test fixture for `scripts/wc-ui-compare.mjs`.
// Plain Bootstrap reference: a single primary button.
//
// Paired with ./wc.astro — both pages render the same UI; the second
// uses our `<bs-button>` web component instead of a native `<button>`.
// The comparator should report 0 style mismatches.
---

<!doctype html>
<html lang="en" data-bs-theme="light">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Smoke reference</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <style>
      body {
        padding: 2rem;
        font-family:
          system-ui,
          -apple-system,
          sans-serif;
      }
    </style>
  </head>
  <body>
    <button
      type="button"
      class="btn btn-primary"
      data-compare-key="cta"
    >
      Get started
    </button>
  </body>
</html>
